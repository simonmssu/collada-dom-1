#!/bin/sh

arg=$1
if [ ! -f "$arg" ] ; then
   exit 1
fi

dir=$(dirname $arg)
if [ ! -z "$dir" ] ; then
	cd $dir
fi

lib=${arg##*/}

lib1=${lib%.*}
ln -s $lib $lib1

lib2=${lib1%.*}
ln -s $lib1 $lib2

if [ ! -z "$dir" ] ; then
	cd -
fi
